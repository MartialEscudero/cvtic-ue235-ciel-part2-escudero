{% extends 'base.html.twig' %}

{% block title %}Modifier le plat - {{ plat.nom }}{% endblock %}

{% block body %}
<h1 class="text-center">Modifier le plat</h1>
{{ include('plat/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
<div class="text-center col-lg-12 mb-3">
    <a class="btn btn-info btn-sm" href="{{ path('plat_index') }}">Retour</a>
</div>
{{ include('plat/_delete_form.html.twig') }}
{% endblock %}
